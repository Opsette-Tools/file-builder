// Opsette Share — per-app configuration for File Builder.
// See ../../../_shared/opsette-share/INTEGRATION.md.

import type { OpsetteShareConfig } from "./config.template";

export type { OpsetteShareConfig };

export const opsetteShareConfig: OpsetteShareConfig = {
  appName: "File Builder",
  tagline: "Package a client brand kit into one clean ZIP, and compress, convert, and resize files — all in your browser.",
  url: "https://tools.opsette.io/file-builder/",
  logoSrc: "opsette-logo.png",
};
